general:
  branches:
    ignore:
      - gh-pages

deployment:
  production:
    branch: master
    commands:
      - npm run build
      - aws s3 sync build/ s3://aws-website-totum-kpl3n.s3-website-us-east-1.amazonaws.com
  staging:
    branch: /^(?!master|candidates\/.*).*$/
    commands:
      - npm run build
      - aws s3 sync build/ s3://aws-website-totum-staging-wkpb2.s3-website-us-east-1.amazonaws.com
